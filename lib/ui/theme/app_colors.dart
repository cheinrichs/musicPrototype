import 'package:flutter/material.dart';

/// Color palette for the ear training app
/// Designed to be playful, kid-friendly, and high contrast
class AppColors {
  AppColors._();

  // Primary colors
  static const Color primary = Color(0xFF6C5CE7);
  static const Color primaryLight = Color(0xFFA29BFE);
  static const Color primaryDark = Color(0xFF5849C7);

  // Secondary colors
  static const Color secondary = Color(0xFF00CEC9);
  static const Color secondaryLight = Color(0xFF81ECEC);

  // Feedback colors
  static const Color correct = Color(0xFF00B894);
  static const Color correctLight = Color(0xFF55EFC4);
  static const Color incorrect = Color(0xFFFF7675);
  static const Color incorrectLight = Color(0xFFFAB1A0);

  // Background colors
  static const Color background = Color(0xFFFFFBF5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF8F4EF);

  // Text colors
  static const Color textPrimary = Color(0xFF2D3436);
  static const Color textSecondary = Color(0xFF636E72);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Game button colors
  static const Color higherButton = Color(0xFFFF6B6B);
  static const Color lowerButton = Color(0xFF4ECDC4);

  // Reward/celebration colors
  static const Color gold = Color(0xFFFFD93D);
  static const Color silver = Color(0xFFC0C0C0);
  static const Color bronze = Color(0xFFCD7F32);

  // Shadow color
  static const Color shadow = Color(0x1A000000);
}
